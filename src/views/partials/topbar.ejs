<header class="topbar">
    <div class="topbar-left">
        <button class="hamburger">☰ Menu</button>
    </div>

    <div class="topbar-right">
        <span class="username"
              hx-get="/me"
              hx-target="#content">
            <%= user.userName %>
        </span>

        <button id="topbarBackBtn">Tilbage</button>
    </div>

    <nav class="dropdown">
        <button class="dropdown-item"
                hx-get="/indexContent"
                hx-target="#content">
            Forside
        </button>

        <button class="dropdown-item"
                hx-get="/customers"
                hx-target="#content">
            Kunder
        </button>

        <button class="dropdown-item"
                hx-get="/cleaningTaskTemplates/page"
                hx-target="#content">
            Opgaver
        </button>

        <% if (user.role === 'admin') { %>
            <button class="dropdown-item"
                    hx-get="/admin"
                    hx-target="#content">
                Admin
            </button>
        <% } %>

        <button class="dropdown-item"
                hx-get="/logout"
                hx-target="#content">
            Log ud
        </button>
    </nav>
</header>
